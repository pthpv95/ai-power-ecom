services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: aishop
      POSTGRES_PASSWORD: aishop
      POSTGRES_DB: aishop
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
